# 安全审计基线（机器全量扫描）

**扫描工具**：`scripts/audit_baseline.py`　**问题总数**：316　（P0 0 / P1 0 / P2 316）


> 本基线由机器全量扫描产出，替代人工抽样。此后不再追加新的检查维度——第六轮整改以本文件为唯一交付标准。

## 一、按检测维度汇总

| 维度 | 说明 | P0 | P1 | P2 | 小计 |
|---|---|---|---|---|---|
| D1 | @action / 自定义方法绕过租户隔离 | 0 | 0 | 132 | 132 |
| D2 | 序列化器敏感字段暴露 | 0 | 0 | 72 | 72 |
| D3 | 生产代码模拟实现残留 | 0 | 0 | 90 | 90 |
| D4 | 视图未接入租户隔离机制 | 0 | 0 | 22 | 22 |

## 二、按模块汇总

| 模块 | P0 | P1 | P2 | 小计 |
|---|---|---|---|---|
| ui_automation | 0 | 0 | 84 | 84 |
| api_testing | 0 | 0 | 43 | 43 |
| testcases | 0 | 0 | 37 | 37 |
| assistant | 0 | 0 | 26 | 26 |
| core_platform | 0 | 0 | 23 | 23 |
| performance_test | 0 | 0 | 20 | 20 |
| reports | 0 | 0 | 16 | 16 |
| strix_security | 0 | 0 | 16 | 16 |
| data_factory | 0 | 0 | 15 | 15 |
| requirement_analysis | 0 | 0 | 12 | 12 |
| knowledge_graph | 0 | 0 | 10 | 10 |
| midscene | 0 | 0 | 6 | 6 |
| executions | 0 | 0 | 3 | 3 |
| reviews | 0 | 0 | 2 | 2 |
| scheduler | 0 | 0 | 2 | 2 |
| defects | 0 | 0 | 1 | 1 |

## 三、P2 明细（316 项）

### D1 · @action / 自定义方法绕过租户隔离

- `apps/api_testing/views/dashboard_views.py:45` **ApiDashboardViewSet.stats @action** — ApiProject.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/api_testing/views/dashboard_views.py:49` **ApiDashboardViewSet.stats @action** — ApiRequest.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/api_testing/views/dashboard_views.py:51` **ApiDashboardViewSet.stats @action** — TestSuite.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/api_testing/views/dashboard_views.py:53` **ApiDashboardViewSet.stats @action** — RequestHistory.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/api_testing/views/dashboard_views.py:55` **ApiDashboardViewSet.stats @action** — ApiTestCaseExecution.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/api_testing/views/execution_views.py:160` **TestSuiteViewSet.add_test_cases @action** — ApiTestCase.objects.get() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/api_testing/views/project_views.py:93` **ApiProjectViewSet.create_sample_project @action** — ApiProject.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/api_testing/views/project_views.py:124` **ApiProjectViewSet.import_api_data @action** — ApiProject.objects.get() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/api_testing/views/project_views.py:142` **ApiProjectViewSet.import_api_data @action** — ApiProject.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/api_testing/views/project_views.py:192` **ApiProjectViewSet.get_import_task_status @action** — ApiImportTask.objects.get() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/api_testing/views/project_views.py:222` **ApiProjectViewSet._import_postman_data** — ApiRequest.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/api_testing/views/project_views.py:223` **ApiProjectViewSet._import_postman_data** — ApiCollection.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/api_testing/views/project_views.py:240` **ApiProjectViewSet._import_postman_data** — ApiCollection.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/api_testing/views/project_views.py:244` **ApiProjectViewSet._import_postman_data** — ApiCollection.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/api_testing/views/project_views.py:254` **ApiProjectViewSet._import_postman_data** — ApiCollection.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/api_testing/views/project_views.py:298` **ApiProjectViewSet._import_postman_data** — ApiRequest.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/api_testing/views/project_views.py:349` **ApiProjectViewSet._import_jmeter_data** — ApiCollection.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/api_testing/views/project_views.py:371` **ApiProjectViewSet._import_jmeter_data** — ApiRequest.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/api_testing/views/project_views.py:422` **ApiProjectViewSet._import_custom_data** — ApiRequest.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/api_testing/views/project_views.py:423` **ApiProjectViewSet._import_custom_data** — ApiCollection.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/api_testing/views/project_views.py:429` **ApiProjectViewSet._import_custom_data** — ApiCollection.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/api_testing/views/project_views.py:484` **ApiProjectViewSet._import_custom_data** — ApiRequest.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/api_testing/views/project_views.py:500` **ApiProjectViewSet._import_custom_data** — ApiCollection.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/api_testing/views/project_views.py:515` **ApiProjectViewSet._import_custom_data** — ApiRequest.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/api_testing/views/project_views.py:529` **ApiProjectViewSet._import_custom_data** — ApiCollection.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/api_testing/views/project_views.py:547` **ApiProjectViewSet._import_custom_data** — ApiRequest.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/api_testing/views/project_views.py:571` **ApiProjectViewSet._import_swagger_data** — ApiCollection.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/api_testing/views/project_views.py:581` **ApiProjectViewSet._import_swagger_data** — ApiRequest.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/api_testing/views/project_views.py:663` **EnvironmentViewSet.activate @action** — Environment.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/api_testing/views/request_views.py:129` **ApiRequestViewSet.execute @action** — GlobalParameter.objects.all() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/api_testing/views/request_views.py:245` **RequestHistoryViewSet.batch_delete @action** — RequestHistory.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/api_testing/views/testcase_views.py:89` **ApiTestCaseModuleViewSet.batch_update_order @action** — ApiTestCaseModule.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/assistant/agent_views.py:24` **AgentExecuteView.post** — AgentProfile.objects.prefetch_related() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/assistant/agent_views.py:102` **AgentExecuteStreamView.post** — AgentProfile.objects.prefetch_related() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/assistant/views.py:126` **ChatViewSet.send_message @action** — AIWorkflowConfig.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/assistant/views.py:128` **ChatViewSet.send_message @action** — AIModelConfig.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/assistant/views.py:332` **ChatViewSet._handle_workflow_request** — AIModelConfig.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/assistant/views.py:484` **KnowledgeGraphViewSet.graph_data @action** — KnowledgeEntity.objects.all() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/assistant/views.py:485` **KnowledgeGraphViewSet.graph_data @action** — KnowledgeRelationship.objects.all() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/assistant/views.py:536` **KnowledgeGraphViewSet.stats @action** — KnowledgeDocument.objects.count() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/assistant/views.py:537` **KnowledgeGraphViewSet.stats @action** — KnowledgeEntity.objects.count() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/assistant/views.py:538` **KnowledgeGraphViewSet.stats @action** — KnowledgeRelationship.objects.count() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/assistant/views.py:539` **KnowledgeGraphViewSet.stats @action** — KnowledgeDocument.objects.exists() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/assistant/views.py:539` **KnowledgeGraphViewSet.stats @action** — KnowledgeDocument.objects.last() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/core_platform/views/configuration.py:31` **GlobalParameterViewSet.destroy** — ApiRequest.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/core_platform/views/configuration.py:47` **GlobalParameterViewSet.destroy** — ApiTestCaseStep.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/core_platform/views/configuration.py:81` **CommonMethodViewSet.destroy** — ApiRequest.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/core_platform/views/configuration.py:93` **CommonMethodViewSet.destroy** — ApiTestCaseStep.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/core_platform/views/projects.py:180` **ProjectEnvironmentListCreateView._user_can_access_project** — Project.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/core_platform/views/projects.py:181` **ProjectEnvironmentListCreateView._user_can_access_project** — Project.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/core_platform/views/versions.py:64` **VersionListCreateView.perform_create** — Project.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/data_factory/views.py:476` **TableMetadataViewSet.list** — VannaConfig.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/data_factory/views.py:679` **TableMetadataViewSet.auto_scan @action** — VannaConfig.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/data_factory/views.py:702` **DataFactoryDashboardViewSet.summary @action** — DataFactoryProject.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/data_factory/views.py:708` **DataFactoryDashboardViewSet.summary @action** — VannaConfig.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/data_factory/views.py:709` **DataFactoryDashboardViewSet.summary @action** — SavedQuery.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/data_factory/views.py:712` **DataFactoryDashboardViewSet.summary @action** — SqlGeneration.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/knowledge_graph/views.py:174` **KnowledgeGraphVizViewSet.entities @action** — KnowledgeEntity.objects.all() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/knowledge_graph/views.py:180` **KnowledgeGraphVizViewSet.relations @action** — KnowledgeRelation.objects.all() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/knowledge_graph/views.py:188` **KnowledgeGraphVizViewSet.visualization_data @action** — KnowledgeEntity.objects.all() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/knowledge_graph/views.py:189` **KnowledgeGraphVizViewSet.visualization_data @action** — KnowledgeRelation.objects.all() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/midscene/views.py:351` **MidsceneDashboardViewSet.summary @action** — MidsceneConfig.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/midscene/views.py:352` **MidsceneDashboardViewSet.summary @action** — MidsceneTask.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/performance_test/views.py:326` **PerformanceEnvironmentViewSet.activate @action** — PerformanceEnvironment.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/performance_test/views.py:416` **PerformanceTestSuiteViewSet.add_requests @action** — PerformanceRequest.objects.get() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/performance_test/views.py:580` **PerformanceDashboardViewSet.summary @action** — PerformanceProject.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/performance_test/views.py:586` **PerformanceDashboardViewSet.summary @action** — PerformanceCollection.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/performance_test/views.py:589` **PerformanceDashboardViewSet.summary @action** — PerformanceRequest.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/performance_test/views.py:592` **PerformanceDashboardViewSet.summary @action** — PerformanceTestSuite.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/performance_test/views.py:595` **PerformanceDashboardViewSet.summary @action** — PerformanceTestExecution.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/performance_test/views.py:600` **PerformanceDashboardViewSet.summary @action** — PerformanceTestExecution.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/reports/views.py:89` **TestReportViewSet.dashboard @action** — TestPlan.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/reports/views.py:90` **TestReportViewSet.dashboard @action** — TestCase.objects.all() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/reports/views.py:117` **TestReportViewSet.dashboard @action** — TestRun.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/reports/views.py:129` **TestReportViewSet.dashboard @action** — TestRun.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/reports/views.py:151` **TestReportViewSet.status_distribution @action** — TestRun.objects.all() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/reports/views.py:157` **TestReportViewSet.status_distribution @action** — TestRunCase.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/reports/views.py:172` **TestReportViewSet.defect_distribution @action** — TestRunCase.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/reports/views.py:195` **TestReportViewSet.failed_cases_top @action** — TestRunCase.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/reports/views.py:226` **TestReportViewSet.execution_trend @action** — TestRunCase.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/reports/views.py:265` **TestReportViewSet.ai_efficiency @action** — TestCase.objects.all() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/reports/views.py:266` **TestReportViewSet.ai_efficiency @action** — GeneratedTestCase.objects.all() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/reports/views.py:267` **TestReportViewSet.ai_efficiency @action** — BusinessRequirement.objects.all() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/reports/views.py:306` **TestReportViewSet.team_workload @action** — TestRunCase.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/reports/views.py:498` **TestReportViewSet.analyze @action** — AIModelConfig.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/requirement_analysis/views.py:698` **TestCaseGenerationTaskViewSet.generate @action** — AIModelConfig.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/requirement_analysis/views.py:702` **TestCaseGenerationTaskViewSet.generate @action** — AIModelConfig.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/requirement_analysis/views.py:719` **TestCaseGenerationTaskViewSet.generate @action** — AIModelConfig.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/requirement_analysis/views.py:723` **TestCaseGenerationTaskViewSet.generate @action** — AIModelConfig.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/requirement_analysis/views.py:761` **TestCaseGenerationTaskViewSet.generate @action** — KnowledgeDocument.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/requirement_analysis/views.py:1069` **TestCaseGenerationTaskViewSet.save_to_records @action** — Project.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/requirement_analysis/views.py:1146` **TestCaseGenerationTaskViewSet.saved_records @action** — TestCaseGenerationTask.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/requirement_analysis/views.py:1207` **TestCaseGenerationTaskViewSet.batch_adopt @action** — Project.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/requirement_analysis/views.py:1300` **TestCaseGenerationTaskViewSet.batch_adopt_selected @action** — Project.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/reviews/views.py:57` **TestCaseReviewViewSet.assign_reviewers @action** — User.objects.get() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/reviews/views.py:116` **TestCaseReviewViewSet.my_reviews @action** — TestCaseReview.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/strix_security/views.py:169` **SecurityTestDashboardViewSet.summary @action** — SecurityTestProject.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/strix_security/views.py:171` **SecurityTestDashboardViewSet.summary @action** — SecurityTestExecution.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/strix_security/views.py:172` **SecurityTestDashboardViewSet.summary @action** — Vulnerability.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/strix_security/views.py:173` **SecurityTestDashboardViewSet.summary @action** — Vulnerability.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/strix_security/views.py:174` **SecurityTestDashboardViewSet.summary @action** — Vulnerability.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/strix_security/views.py:175` **SecurityTestDashboardViewSet.summary @action** — Vulnerability.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/strix_security/views.py:176` **SecurityTestDashboardViewSet.summary @action** — Vulnerability.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/strix_security/views.py:177` **SecurityTestDashboardViewSet.summary @action** — Vulnerability.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/strix_security/views.py:179` **SecurityTestDashboardViewSet.summary @action** — SecurityTestExecution.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/strix_security/views.py:180` **SecurityTestDashboardViewSet.summary @action** — Vulnerability.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/testcases/views.py:114` **TestCaseListCreateView.get_user_accessible_projects** — Project.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/testcases/views.py:180` **TestCaseDetailView.get_user_accessible_projects** — Project.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/testcases/views.py:221` **ImportTestCaseView.post** — Project.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/testcases/views.py:241` **ExportTestCaseView.get** — TestCase.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/ui_automation/debug_views.py:33` **DebugFileViewSet.list** — UiProject.objects.get() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/ui_automation/debug_views.py:43` **DebugFileViewSet.list** — TestCase.objects.get() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/ui_automation/views/ai_views.py:624` **AIExecutionRecordViewSet.run_agent_browser @action** — UiProject.objects.first() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/ui_automation/views/ai_views.py:735` **AIExecutionRecordViewSet.inspect_page @action** — AIModelConfig.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/ui_automation/views/dashboard_views.py:99` **UiDashboardViewSet.stats @action** — UiProject.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/ui_automation/views/dashboard_views.py:103` **UiDashboardViewSet.stats @action** — TestCase.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/ui_automation/views/dashboard_views.py:105` **UiDashboardViewSet.stats @action** — TestSuite.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/ui_automation/views/dashboard_views.py:107` **UiDashboardViewSet.stats @action** — TestExecution.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/ui_automation/views/dashboard_views.py:109` **UiDashboardViewSet.stats @action** — TestCaseExecution.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/ui_automation/views/script_views.py:159` **ScriptElementUsageViewSet._analyze_script_elements** — Element.objects.get() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/ui_automation/views/task_views.py:186` **UiScheduledTaskViewSet.run_now @action** — TestCase.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/ui_automation/views/task_views.py:479` **UiScheduledTaskViewSet._send_webhook_notification** — NotificationConfig.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/ui_automation/views/task_views.py:706` **NotificationConfigViewSet.set_default @action** — NotificationConfig.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/ui_automation/views/testcase_views.py:151` **UiTestCaseModuleViewSet.batch_update_order @action** — UiTestCaseModule.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/ui_automation/views/testcase_views.py:267` **TestCaseViewSet._process_steps** — LocatorStrategy.objects.get() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/ui_automation/views/testcase_views.py:270` **TestCaseViewSet._process_steps** — LocatorStrategy.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/ui_automation/views/testcase_views.py:273` **TestCaseViewSet._process_steps** — LocatorStrategy.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/ui_automation/views/testcase_views.py:276` **TestCaseViewSet._process_steps** — Element.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/ui_automation/views/testcase_views.py:318` **TestCaseViewSet._process_steps** — PageObjectElement.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/ui_automation/views/testcase_views.py:321` **TestCaseViewSet._process_steps** — PageObjectElement.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/ui_automation/views/testcase_views.py:343` **TestCaseViewSet._process_steps** — TestCaseStep.objects.filter() 绕过 get_queryset，租户过滤依赖来源待人工确认
- `apps/ui_automation/views/testcase_views.py:1230` **TestCaseViewSet.batch_run @action** — TestCase.objects.get() 绕过 get_queryset，租户过滤依赖来源待人工确认
### D2 · 序列化器敏感字段暴露

- `apps/api_testing/serializers.py:17` **UserSerializer(model=User)** — PII 字段 ['email'] 已声明掩码（PIIMaskMixin，本人/管理员明文，他人掩码）
- `apps/api_testing/serializers.py:845` **NotificationLogDetailSerializer(model=NotificationLog)** — PII 字段 ['sender_email'] 已声明掩码（PIIMaskMixin，本人/管理员明文，他人掩码）
- `apps/assistant/serializers.py:76` **KnowledgeDocumentSerializer(model=KnowledgeDocument)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/assistant/serializers.py:82` **KnowledgeEntitySerializer(model=KnowledgeEntity)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/assistant/serializers.py:88` **KnowledgeRelationshipSerializer(model=KnowledgeRelationship)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/assistant/serializers.py:94` **AgentSkillSerializer(model=AgentSkill)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/core_platform/serializers/configuration.py:4` **GlobalParameterSerializer(model=GlobalParameter)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/core_platform/serializers/configuration.py:10` **CommonMethodSerializer(model=CommonMethod)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/core_platform/serializers/projects.py:10` **ProjectEnvironmentSerializer(model=ProjectEnvironment)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/core_platform/serializers/users.py:16` **UserSimpleSerializer(model=User)** — PII 字段 ['email'] 已声明掩码（PIIMaskMixin，本人/管理员明文，他人掩码）
- `apps/core_platform/serializers/users.py:24` **UserSerializer(model=User)** — PII 字段 ['email', 'phone'] 已声明掩码（PIIMaskMixin，本人/管理员明文，他人掩码）
- `apps/data_factory/serializers.py:18` **UserSerializer(model=User)** — PII 字段 ['email'] 已声明掩码（PIIMaskMixin，本人/管理员明文，他人掩码）
- `apps/data_factory/serializers.py:28` **VannaConfigSerializer(model=VannaConfig)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/data_factory/serializers.py:71` **SqlGenerationSerializer(model=SqlGeneration)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/data_factory/serializers.py:81` **DataFactoryProjectSerializer(model=DataFactoryProject)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/data_factory/serializers.py:92` **SavedQuerySerializer(model=SavedQuery)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/data_factory/serializers.py:106` **QueryHistorySerializer(model=QueryHistory)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/data_factory/serializers.py:133` **DataSourceSerializer(model=DataSource)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/data_factory/serializers.py:153` **DataPoolSerializer(model=DataPool)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/executions/serializers.py:52` **TestPlanDetailSerializer(model=TestPlan)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/executions/serializers.py:62` **TestRunCaseSerializer(model=TestRunCase)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/knowledge_graph/serializers.py:4` **KnowledgeDocumentSerializer(model=KnowledgeDocument)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/knowledge_graph/serializers.py:12` **DocumentChunkSerializer(model=DocumentChunk)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/knowledge_graph/serializers.py:17` **KnowledgeEntitySerializer(model=KnowledgeEntity)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/knowledge_graph/serializers.py:22` **KnowledgeRelationSerializer(model=KnowledgeRelation)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/midscene/serializers.py:13` **UserSerializer(model=User)** — PII 字段 ['email'] 已声明掩码（PIIMaskMixin，本人/管理员明文，他人掩码）
- `apps/midscene/serializers.py:23` **MidsceneConfigSerializer(model=MidsceneConfig)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/midscene/serializers.py:35` **MidsceneTaskSerializer(model=MidsceneTask)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/midscene/serializers.py:48` **MidsceneExecutionLogSerializer(model=MidsceneExecutionLog)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/performance_test/serializers.py:20` **UserSerializer(model=User)** — PII 字段 ['email'] 已声明掩码（PIIMaskMixin，本人/管理员明文，他人掩码）
- `apps/performance_test/serializers.py:30` **PerformanceProjectSerializer(model=PerformanceProject)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/performance_test/serializers.py:40` **PerformanceCollectionSerializer(model=PerformanceCollection)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/performance_test/serializers.py:50` **PerformanceRequestSerializer(model=PerformanceRequest)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/performance_test/serializers.py:60` **PerformanceEnvironmentSerializer(model=PerformanceEnvironment)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/performance_test/serializers.py:70` **PerformanceTestSuiteRequestSerializer(model=PerformanceTestSuiteRequest)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/performance_test/serializers.py:80` **PerformanceTestSuiteSerializer(model=PerformanceTestSuite)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/performance_test/serializers.py:92` **PerformanceTestExecutionSerializer(model=PerformanceTestExecution)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/performance_test/serializers.py:102` **PerformanceTestHistorySerializer(model=PerformanceTestHistory)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/performance_test/serializers.py:113` **PerformanceScheduledTaskSerializer(model=PerformanceScheduledTask)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/performance_test/serializers.py:123` **PerformanceTaskExecutionLogSerializer(model=PerformanceTaskExecutionLog)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/reports/serializers.py:5` **ReportTemplateSerializer(model=ReportTemplate)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/scheduler/serializers.py:25` **ScheduledTaskSerializer(model=ScheduledTask)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/scheduler/serializers.py:43` **TaskExecutionLogSerializer(model=TaskExecutionLog)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/strix_security/serializers.py:15` **UserSerializer(model=User)** — PII 字段 ['email'] 已声明掩码（PIIMaskMixin，本人/管理员明文，他人掩码）
- `apps/strix_security/serializers.py:25` **StrixConfigSerializer(model=StrixConfig)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/strix_security/serializers.py:37` **SecurityTestProjectSerializer(model=SecurityTestProject)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/strix_security/serializers.py:47` **SecurityTestExecutionSerializer(model=SecurityTestExecution)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/strix_security/serializers.py:57` **VulnerabilitySerializer(model=Vulnerability)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/strix_security/serializers.py:66` **SecurityTestHistorySerializer(model=SecurityTestHistory)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/testcases/serializers.py:6` **TestCaseStepSerializer(model=TestCaseStep)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/testcases/serializers.py:11` **TestCaseAttachmentSerializer(model=TestCaseAttachment)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/testcases/serializers.py:18` **TestCaseCommentSerializer(model=TestCaseComment)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/testcases/serializers.py:51` **TestCaseSerializer(model=TestCase)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/ui_automation/serializers/element.py:17` **LocatorStrategySerializer(model=LocatorStrategy)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/ui_automation/serializers/element.py:23` **ElementSerializer(model=Element)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/ui_automation/serializers/element.py:99` **ElementGroupSerializer(model=ElementGroup)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/ui_automation/serializers/element.py:126` **ElementEnhancedSerializer(model=Element)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/ui_automation/serializers/element.py:227` **PageObjectSerializer(model=PageObject)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/ui_automation/serializers/element.py:266` **PageObjectElementSerializer(model=PageObjectElement)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/ui_automation/serializers/execution.py:49` **TestSuiteSerializer(model=TestSuite)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/ui_automation/serializers/execution.py:85` **TestSuiteWithScriptsSerializer(model=TestSuite)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/ui_automation/serializers/execution.py:94` **TestExecutionSerializer(model=TestExecution)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/ui_automation/serializers/execution.py:136` **ScreenshotSerializer(model=Screenshot)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/ui_automation/serializers/project.py:18` **UserSerializer(model=User)** — PII 字段 ['email'] 已声明掩码（PIIMaskMixin，本人/管理员明文，他人掩码）
- `apps/ui_automation/serializers/project.py:27` **UiProjectSerializer(model=UiProject)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/ui_automation/serializers/project.py:49` **UiDeviceSerializer(model=UiDevice)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/ui_automation/serializers/project.py:61` **ExecutionNodeSerializer(model=ExecutionNode)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/ui_automation/serializers/script.py:18` **TestScriptSerializer(model=TestScript)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/ui_automation/serializers/script.py:40` **ScriptStepSerializer(model=ScriptStep)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/ui_automation/serializers/script.py:67` **ScriptElementUsageSerializer(model=ScriptElementUsage)** — fields='__all__' 全字段输出，建议显式白名单
- `apps/ui_automation/serializers/task.py:222` **NotificationLogSerializer(model=NotificationLog)** — PII 字段 ['sender_email'] 已声明掩码（PIIMaskMixin，本人/管理员明文，他人掩码）
- `apps/ui_automation/serializers/testcase.py:156` **AICaseSerializer(model=AICase)** — fields='__all__' 全字段输出，建议显式白名单
### D3 · 生产代码模拟实现残留

- `apps/api_testing/management/commands/run_api_scheduler.py:56` **time.sleep 伪装耗时** — 生产代码疑似模拟实现: time.sleep(10)
- `apps/api_testing/utils/requests_runner.py:404` **time.sleep 伪装耗时** — 生产代码疑似模拟实现: time.sleep(step.wait_time / 1000.0)
- `apps/api_testing/utils/variables.py:126` **random 造数** — 已声明为真实现（audit: real-impl）：数据池随机取值是造数功能本身（${pool.名称.字段} 变量解析），非伪造执行结果｜命中行: row = random.choice(pool.data)
- `apps/api_testing/views/execution_views.py:287` **time.sleep 伪装耗时** — 生产代码疑似模拟实现: time.sleep(1)
- `apps/assistant/agent_factory.py:50` **mock 数据/结果** — 已声明为真实现（audit: real-impl）：system_prompt 文案描述该 Agent 的职责（供给测试数据），非伪造实现｜命中行: system_prompt="You are a Data Engineering AI. You understand
- `apps/assistant/services.py:92` **Mock 降级模式** — 关键词命中于注释/文档字符串（非可执行代码）: """调用真实 AI 引擎抽取实体和关系；仅当无任何可用大模型配置时降级为 mock（明确标注）。"""
- `apps/assistant/services.py:113` **Mock 降级模式** — 关键词命中于注释/文档字符串（非可执行代码）: # 仅当完全无可用的大模型配置时才降级 mock
- `apps/assistant/services.py:117` **Mock 降级模式** — 已声明为真实现（audit: real-impl）：降级路径的告警日志；降级产物已按口径强制打标 demo_ 前缀 + [演示数据] 描述，graph_data 接口返回 is_demo 与 demo_data_warning，用户可辨识｜命中行: logger.warning("知识图谱：未配置激活的 AI 模型，使用 mock 抽取（演示数据）。")
- `apps/assistant/services.py:166` **假数据/伪造** — 关键词命中于注释/文档字符串（非可执行代码）: # 已有可用的大模型配置却调用失败：绝不静默造"演示"假数据污染知识库，
- `apps/assistant/services.py:169` **假数据/伪造** — 关键词命中于注释/文档字符串（非可执行代码）: # audit: real-impl 错误日志文案（说明"拒绝生成假数据"的整改行为本身），非造假实现
- `apps/assistant/services.py:170` **假数据/伪造** — 已声明为真实现（audit: real-impl）：错误日志文案（说明"拒绝生成假数据"的整改行为本身），非造假实现｜命中行: "知识图谱 LLM 抽取失败（已有配置，拒绝生成假数据，错误已暴露）：doc_id=%s 错误=%s",
- `apps/assistant/skills_library.py:31` **假数据/伪造** — 关键词命中于注释/文档字符串（非可执行代码）: # 原实现伪造 Playwright 执行结果与假截图，已按整改要求移除
- `apps/data_factory/views.py:80` **假数据/伪造** — 关键词命中于注释/文档字符串（非可执行代码）: """真实执行 SQL 并返回结构化结果（替代原模拟假数据），失败抛异常。"""
- `apps/executions/k8s_service.py:11` **假数据/伪造** — 关键词命中于注释/文档字符串（非可执行代码）: 及假日志，会被误认为真实执行结果，已按整改要求移除。
- `apps/knowledge_graph/connectors/aliyun.py:5` **假数据/伪造** — 关键词命中于注释/文档字符串（非可执行代码）: 真实对接能力本期未交付；原 mock 假数据已按整改要求移除，
- `apps/knowledge_graph/connectors/youdao.py:5` **假数据/伪造** — 关键词命中于注释/文档字符串（非可执行代码）: 真实对接能力本期未交付；原 mock 假数据已按整改要求移除，
- `apps/reports/views.py:544` **time.sleep 伪装耗时** — 已声明为真实现（audit: real-impl）：AI 调用失败后的重试退避等待，非伪装耗时｜命中行: time.sleep(1) # 短暂亦避让
- `apps/requirement_analysis/views.py:67` **中文"模拟X"** — 关键词命中于注释/文档字符串（非可执行代码）: # 执行真实分析（原为写死的模拟分析结果，已按整改要求移除）
- `apps/requirement_analysis/views.py:344` **中文"模拟X"** — 关键词命中于注释/文档字符串（非可执行代码）: # 执行真实分析（原为写死的模拟分析结果，已按整改要求移除）
- `apps/requirement_analysis/views.py:462` **假数据/伪造** — 关键词命中于注释/文档字符串（非可执行代码）: # 分析失败时如实暴露错误，不再静默回退到伪造的"备用分析"假数据
- `apps/testcases/management/commands/init_ui_automation_data.py:203` **random 造数** — 生产代码疑似模拟实现: step_count = random.randint(2, 5)
- `apps/testcases/management/commands/init_ui_automation_data.py:208` **random 造数** — 生产代码疑似模拟实现: action_type=random.choice(['click', 'fill', 'wait', 'assert']),
- `apps/testcases/management/commands/init_ui_automation_data.py:209` **random 造数** — 生产代码疑似模拟实现: element=random.choice(elements) if random.random() > 0.3 else None,
- `apps/testcases/management/commands/init_ui_automation_data.py:210` **random 造数** — 生产代码疑似模拟实现: input_value=f'test_value_{i}' if random.choice(['click', 'fill', 'wait', 'assert']) == 'fi
- `apps/testcases/management/commands/init_ui_automation_data.py:211` **random 造数** — 生产代码疑似模拟实现: wait_time=random.randint(500, 2000),
- `apps/testcases/management/commands/init_ui_automation_data.py:212` **random 造数** — 生产代码疑似模拟实现: assert_type=random.choice(['equals', 'contains', 'exists']) if random.choice(['click', 'fi
- `apps/testcases/management/commands/init_ui_automation_data.py:213` **random 造数** — 生产代码疑似模拟实现: assert_value=f'expected_{i}' if random.choice(['click', 'fill', 'wait', 'assert']) == 'ass
- `apps/testcases/management/commands/init_ui_automation_data.py:239` **random 造数** — 生产代码疑似模拟实现: suite_test_cases = random.sample(test_cases, random.randint(1, 3))
- `apps/testcases/management/commands/init_ui_automation_data.py:251` **random 造数** — 生产代码疑似模拟实现: test_suite = random.choice(test_suites)
- `apps/testcases/management/commands/init_ui_automation_data.py:257` **random 造数** — 生产代码疑似模拟实现: 'status': random.choice(['SUCCESS', 'FAILED', 'ABORTED']),
- `apps/testcases/management/commands/init_ui_automation_data.py:258` **random 造数** — 生产代码疑似模拟实现: 'started_at': timezone.now() - timezone.timedelta(minutes=random.randint(10, 120)),
- `apps/testcases/management/commands/init_ui_automation_data.py:259` **random 造数** — 生产代码疑似模拟实现: 'finished_at': timezone.now() - timezone.timedelta(minutes=random.randint(5, 60)),
- `apps/testcases/management/commands/init_ui_automation_data.py:260` **random 造数** — 生产代码疑似模拟实现: 'duration': random.randint(30, 300),
- `apps/testcases/management/commands/init_ui_automation_data.py:261` **random 造数** — 生产代码疑似模拟实现: 'engine': random.choice(['playwright', 'selenium']),
- `apps/testcases/management/commands/init_ui_automation_data.py:262` **random 造数** — 生产代码疑似模拟实现: 'browser': random.choice(['chrome', 'firefox', 'edge']),
- `apps/testcases/management/commands/init_ui_automation_data.py:267` **random 造数** — 生产代码疑似模拟实现: test_script = random.choice(scripts)
- `apps/testcases/management/commands/init_ui_automation_data.py:273` **random 造数** — 生产代码疑似模拟实现: 'status': random.choice(['SUCCESS', 'FAILED', 'ABORTED']),
- `apps/testcases/management/commands/init_ui_automation_data.py:274` **random 造数** — 生产代码疑似模拟实现: 'started_at': timezone.now() - timezone.timedelta(minutes=random.randint(10, 120)),
- `apps/testcases/management/commands/init_ui_automation_data.py:275` **random 造数** — 生产代码疑似模拟实现: 'finished_at': timezone.now() - timezone.timedelta(minutes=random.randint(5, 60)),
- `apps/testcases/management/commands/init_ui_automation_data.py:276` **random 造数** — 生产代码疑似模拟实现: 'duration': random.randint(30, 300),
- `apps/testcases/management/commands/init_ui_automation_data.py:277` **random 造数** — 生产代码疑似模拟实现: 'engine': random.choice(['playwright', 'selenium']),
- `apps/testcases/management/commands/init_ui_automation_data.py:278` **random 造数** — 生产代码疑似模拟实现: 'browser': random.choice(['chrome', 'firefox', 'edge']),
- `apps/testcases/management/commands/init_ui_automation_data.py:285` **random 造数** — 生产代码疑似模拟实现: test_case = random.choice(test_cases)
- `apps/testcases/management/commands/init_ui_automation_data.py:291` **random 造数** — 生产代码疑似模拟实现: 'status': random.choice(['passed', 'failed', 'error']),
- `apps/testcases/management/commands/init_ui_automation_data.py:292` **random 造数** — 生产代码疑似模拟实现: 'started_at': timezone.now() - timezone.timedelta(minutes=random.randint(10, 120)),
- `apps/testcases/management/commands/init_ui_automation_data.py:293` **random 造数** — 生产代码疑似模拟实现: 'finished_at': timezone.now() - timezone.timedelta(minutes=random.randint(5, 60)),
- `apps/testcases/management/commands/init_ui_automation_data.py:294` **random 造数** — 生产代码疑似模拟实现: 'execution_time': random.randint(30, 300),
- `apps/testcases/management/commands/init_ui_automation_data.py:295` **random 造数** — 生产代码疑似模拟实现: 'engine': random.choice(['playwright', 'selenium']),
- `apps/testcases/management/commands/init_ui_automation_data.py:296` **random 造数** — 生产代码疑似模拟实现: 'browser': random.choice(['chrome', 'firefox', 'edge']),
- `apps/ui_automation/ai_vision_web.py:380` **TODO 未实现** — 关键词命中于注释/文档字符串（非可执行代码）: # TODO: Implement actual JS injection to draw bounding boxes with IDs
- `apps/ui_automation/appium_engine.py:234` **time.sleep 伪装耗时** — 生产代码疑似模拟实现: time.sleep(min(10, step_data.get('wait_time', 1000) / 1000.0))
- `apps/ui_automation/appium_engine.py:247` **time.sleep 伪装耗时** — 生产代码疑似模拟实现: time.sleep(step_data.get('wait_time', 500) / 1000.0)
- `apps/ui_automation/executor/playwright_runner.py:173` **中文"模拟X"** — 关键词命中于注释/文档字符串（非可执行代码）: # 尝试获取 Playwright 中的设备模拟数据
- `apps/ui_automation/executor/playwright_runner.py:209` **time.sleep 伪装耗时** — 生产代码疑似模拟实现: time.sleep(extra_wait)
- `apps/ui_automation/executor/selenium_runner.py:216` **time.sleep 伪装耗时** — 生产代码疑似模拟实现: time.sleep(extra_wait)
- `apps/ui_automation/executor/selenium_runner.py:549` **time.sleep 伪装耗时** — 生产代码疑似模拟实现: time.sleep(0.8)  # 等待800ms，确保下拉框完全展开
- `apps/ui_automation/executor/selenium_runner.py:551` **time.sleep 伪装耗时** — 生产代码疑似模拟实现: time.sleep(0.3)  # 其他操作等待300ms
- `apps/ui_automation/executor/selenium_runner.py:746` **time.sleep 伪装耗时** — 生产代码疑似模拟实现: time.sleep(0.5)
- `apps/ui_automation/executor/selenium_runner.py:748` **time.sleep 伪装耗时** — 生产代码疑似模拟实现: time.sleep(0.5)
- `apps/ui_automation/executor/selenium_runner.py:772` **time.sleep 伪装耗时** — 生产代码疑似模拟实现: time.sleep(wait_time)
- `apps/ui_automation/executor/selenium_runner.py:779` **time.sleep 伪装耗时** — 生产代码疑似模拟实现: time.sleep(0.3)
- `apps/ui_automation/executor/selenium_runner.py:786` **time.sleep 伪装耗时** — 生产代码疑似模拟实现: time.sleep(0.3)  # 等待滚动完成
- `apps/ui_automation/executor/selenium_runner.py:822` **time.sleep 伪装耗时** — 生产代码疑似模拟实现: time.sleep(0.5)
- `apps/ui_automation/executor/selenium_runner.py:855` **time.sleep 伪装耗时** — 生产代码疑似模拟实现: time.sleep(wait_time)
- `apps/ui_automation/executor/selenium_runner.py:857` **time.sleep 伪装耗时** — 生产代码疑似模拟实现: time.sleep(0.3)  # 确保元素状态稳定
- `apps/ui_automation/executor/selenium_runner.py:883` **time.sleep 伪装耗时** — 生产代码疑似模拟实现: time.sleep(wait_time)
- `apps/ui_automation/executor/selenium_runner.py:885` **time.sleep 伪装耗时** — 生产代码疑似模拟实现: time.sleep(0.3)
- `apps/ui_automation/executor/selenium_runner.py:908` **time.sleep 伪装耗时** — 生产代码疑似模拟实现: time.sleep(wait_time)
- `apps/ui_automation/executor/selenium_runner.py:910` **time.sleep 伪装耗时** — 生产代码疑似模拟实现: time.sleep(0.3)  # 确保元素状态稳定
- `apps/ui_automation/executor/selenium_runner.py:928` **time.sleep 伪装耗时** — 生产代码疑似模拟实现: time.sleep(wait_time)
- `apps/ui_automation/executor/selenium_runner.py:930` **time.sleep 伪装耗时** — 生产代码疑似模拟实现: time.sleep(0.3)  # 确保元素状态稳定
- `apps/ui_automation/executor/selenium_runner.py:978` **time.sleep 伪装耗时** — 生产代码疑似模拟实现: time.sleep(step_data['wait_time'] / 1000)
- `apps/ui_automation/minium_engine.py:12` **mock 假实例/假对象** — 关键词命中于注释/文档字符串（非可执行代码）: 引擎会静默降级为 "mock_instance"，随后所有步骤一律返回 True 与
- `apps/ui_automation/minium_engine.py:13` **Mock 结果标记** — 关键词命中于注释/文档字符串（非可执行代码）: "✓ [Mock] ..." 日志——用户在平台上看到小程序用例"全部通过"，
- `apps/ui_automation/minium_engine.py:38` **中文"模拟X"** — 生产代码疑似模拟实现: "（本平台不会以模拟结果冒充真实执行）"
- `apps/ui_automation/minium_engine.py:52` **中文"模拟X"** — 生产代码疑似模拟实现: "（本平台不会以模拟结果冒充真实执行）"
- `apps/ui_automation/minium_engine.py:120` **time.sleep 伪装耗时** — 已声明为真实现（audit: real-impl）：wait 动作的真实等待，时长来自用例配置 step.wait_time｜命中行: time.sleep(wait_seconds)
- `apps/ui_automation/selenium_engine.py:385` **time.sleep 伪装耗时** — 已声明为真实现（audit: real-impl）：wait 动作的真实等待，时长来自用例配置 step.wait_time｜命中行: time.sleep(wait_seconds)
- `apps/ui_automation/selenium_engine.py:443` **time.sleep 伪装耗时** — 生产代码疑似模拟实现: time.sleep(0.5)
- `apps/ui_automation/selenium_engine.py:554` **time.sleep 伪装耗时** — 生产代码疑似模拟实现: time.sleep(0.3)  # 等待滚动完成
- `apps/ui_automation/selenium_engine.py:569` **time.sleep 伪装耗时** — 生产代码疑似模拟实现: time.sleep(wait_time)
- `apps/ui_automation/selenium_engine.py:584` **time.sleep 伪装耗时** — 生产代码疑似模拟实现: time.sleep(0.3)
- `apps/ui_automation/selenium_engine.py:603` **time.sleep 伪装耗时** — 已声明为真实现（audit: real-impl）：真实页面交互后的稳定等待（DOM 渲染），非伪装耗时｜命中行: time.sleep(0.5)
- `apps/ui_automation/selenium_engine.py:651` **time.sleep 伪装耗时** — 已声明为真实现（audit: real-impl）：真实页面交互后的稳定等待（DOM 渲染），非伪装耗时｜命中行: time.sleep(0.3)
- `apps/ui_automation/selenium_engine.py:660` **time.sleep 伪装耗时** — 生产代码疑似模拟实现: time.sleep(wait_time)
- `apps/ui_automation/selenium_engine.py:662` **time.sleep 伪装耗时** — 生产代码疑似模拟实现: time.sleep(0.3)  # 确保元素状态稳定
- `apps/ui_automation/selenium_engine.py:686` **time.sleep 伪装耗时** — 生产代码疑似模拟实现: time.sleep(wait_time)
- `apps/ui_automation/selenium_engine.py:688` **time.sleep 伪装耗时** — 生产代码疑似模拟实现: time.sleep(0.3)  # 确保元素状态稳定
- `apps/ui_automation/selenium_engine.py:736` **time.sleep 伪装耗时** — 已声明为真实现（audit: real-impl）：等待浏览器滚动动画完成后再截图，真实需要｜命中行: time.sleep(0.3)  # 等待滚动完成
- `apps/ui_automation/selenium_engine.py:1107` **time.sleep 伪装耗时** — 已声明为真实现（audit: real-impl）：用例显式配置的额外等待时长 extra_wait，真实需要｜命中行: time.sleep(extra_wait)
### D4 · 视图未接入租户隔离机制

- `apps/api_testing/views/project_views.py:615` **EnvironmentViewSet** — 已显式声明自管租户过滤（tenant_scope_self_managed=True），理由：get_queryset 自管归属边界：LOCAL 环境限 Q(owner=user)|Q(members=user) 的 ApiProject（协作成员可见性不变）；GLOBAL 环境本轮由“全平台任意登录用户可读”收敛为“本人或与本人共享 ApiProject 的协作者所创建”，只收紧不放宽。Environment 无 organization 字段且 ApiProject 亦无 organization，自动解析会命中 project__organization 直接 FieldError；退而按 created_by 叠加又会把项目成员可见的 LOCAL 环境收成 owner-only、破坏协作，故声明自管
- `apps/api_testing/views/project_views.py:676` **UserViewSet** — 已显式声明自管租户过滤（tenant_scope_self_managed=True），理由：get_queryset 已实现严于组织级隔离的边界：管理员全量，其余用户仅能看到自己一行（fail-closed）。User 模型自带 organization 字段，若改用自动解析会放宽为“同组织用户互相可见”，直接暴露 email 等 PII，属可见性倒退，故声明自管
- `apps/api_testing/views/request_views.py:53` **ApiRequestViewSet** — 已显式声明自管租户过滤（tenant_scope_self_managed=True），理由：get_queryset 以 collection__project__in = Q(owner=user)|Q(members=user) 的 ApiProject 做归属过滤，未命中即空集（fail-closed）；query_params 的 project 只在该已收敛集合上二次收窄，传他人 project id 也读不到数据。ApiRequest 无 organization、亦无 project 直连字段，自动解析会落到 created_by，把项目成员共享的接口收成 creator-only、破坏协作，故声明自管
- `apps/api_testing/views/task_views.py:596` **NotificationLogViewSet** — 已显式声明自管租户过滤（tenant_scope_self_managed=True），理由：已核实 NotificationLog.task_id 是裸 IntegerField（无外键，见 apps/notifications/models.py:77），模型无 organization / project / created_by 任一租户字段，自动解析必然 fail-closed 清零、让用户看不到自己任务的通知；写 task__xxx 多跳路径则直接 FieldError 500。故由 get_queryset 反查归属：先取 Q(owner=user)|Q(members=user) 的 ApiProject，再取其下 TestSuite / ApiCollection 关联的 ScheduledTask 以及 created_by=user 的任务，按 task_id__in 收敛，未命中即空集（fail-closed），边界不弱于组织级隔离
- `apps/api_testing/views/testcase_views.py:37` **ApiTestCaseModuleViewSet** — 已显式声明自管租户过滤（tenant_scope_self_managed=True），理由：get_queryset 以 super().get_queryset() 起步，完整继承 BaseProjectViewSet 的归属边界：携带 project_id 时校验该项目的 owner/members（传他人 project_id 返回空集），未携带时因 ApiTestCaseModule 无 created_by 字段而直接 none()，两路均 fail-closed；其后仅追加 parent__isnull 层级筛选，不放宽任何可见性。模型有 project 外键但 ApiProject 无 organization 字段，自动解析会命中 project__organization 导致 FieldError，故声明自管
- `apps/assistant/agent_config_views.py:8` **AgentSkillViewSet** — 已显式声明租户豁免（tenant_scope_exempt=True），理由：平台级 Agent 技能字典表，模型无租户字段，全租户共享只读；写操作已由 IsAdminOrReadOnly 限制为管理员
- `apps/assistant/agent_config_views.py:24` **AgentProfileViewSet** — 已显式声明租户豁免（tenant_scope_exempt=True），理由：平台级 Agent 专家画像（name 全局唯一、无租户字段），全租户共享使用；不含密钥类敏感数据
- `apps/core_platform/views/projects.py:17` **ProjectListCreateView** — 已显式声明自管租户过滤（tenant_scope_self_managed=True），理由：自定义 get_queryset 以 Q(owner=user)|Q(members=user) 按项目归属过滤：用户只能看到自己拥有或被邀请加入的项目，未命中即空集（fail-closed），边界不弱于组织级隔离；Project.organization 可为空且注册用户默认无组织，叠加自动 org 过滤会把无组织用户的自有项目清零，并把跨组织协作成员误伤为不可见
- `apps/core_platform/views/projects.py:71` **ProjectDetailView** — 已显式声明自管租户过滤（tenant_scope_self_managed=True），理由：自定义 get_queryset 以 Q(owner=user)|Q(members=user) 按项目归属过滤，非归属项目直接不可达（fail-closed），边界不弱于组织级隔离；Project.organization 可为空，叠加自动 org 过滤会清零无组织用户对自有项目的访问，并阻断跨组织协作成员，属功能倒退。管理员全量可见沿用原有语义
- `apps/core_platform/views/projects.py:164` **ProjectEnvironmentListCreateView** — 已显式声明自管租户过滤（tenant_scope_self_managed=True），理由：get_queryset 先经 _user_can_access_project() 校验 URL 上的 project_id 是否属于当前用户（owner|members，管理员放行），校验不通过直接返回 none()，用户拿别人的 project_id 也读不到环境配置；该归属校验不弱于 project__organization 过滤，且 Project.organization 可为空，叠加自动过滤会误伤无组织用户与跨组织协作成员
- `apps/core_platform/views/rbac.py:21` **RoleViewSet** — 已显式声明租户豁免（tenant_scope_exempt=True），理由：Role 为平台级角色字典表（无 organization 字段、code 全局唯一），全租户共享读取；写操作已由 IsTenantAdminOrReadOnly 限制为管理员
- `apps/core_platform/views/rbac.py:33` **PermissionViewSet** — 已显式声明租户豁免（tenant_scope_exempt=True），理由：Django 内置 auth.Permission 权限字典表，平台级公共只读数据，无租户字段；视图为 ReadOnlyModelViewSet 且需全量供角色编辑器勾选
- `apps/core_platform/views/rbac.py:47` **UserRoleViewSet** — 已显式声明自管租户过滤（tenant_scope_self_managed=True），理由：自定义 get_queryset 已按主体身份分级收敛用户可见范围：平台管理员全量、租户管理员限本组织（无组织时仅自己）、普通用户仅 id=自己，严于组织级隔离；若叠加自动解析（User 模型会被解析为 organization 过滤），普通用户将能看到同组织其他人的 PII，属可见性放宽，绝不可接受
- `apps/core_platform/views/users.py:61` **RegisterView** — 已显式声明租户豁免（tenant_scope_exempt=True），理由：公开注册端点（AllowAny），匿名请求无租户上下文；CreateAPIView 仅创建不列举，queryset 为框架占位，无跨租户数据读取面
- `apps/core_platform/views/users.py:151` **UserListView** — 已显式声明自管租户过滤（tenant_scope_self_managed=True），理由：自定义 get_queryset 对非管理员只返回 id=自己这一行（管理员全量，沿用原语义），是最严的用户级边界；User 模型会被自动解析为 organization 过滤，一旦叠加，同组织用户即可互相列出对方的邮箱等 PII，相对原"仅可见自己"是可见性放宽；且注册用户默认无组织，org 过滤还会让其连自己都看不到
- `apps/core_platform/views/users.py:173` **UserDetailView** — 已显式声明自管租户过滤（tenant_scope_self_managed=True），理由：同 UserListView：非管理员仅能取到 id=自己的对象，他人 id 一律 404（fail-closed），严于组织级隔离；叠加自动 organization 过滤会让同组织用户互相读取详情 PII（放宽），并使无组织用户读不到自己
- `apps/core_platform/views/versions.py:13` **VersionListCreateView** — 已显式声明自管租户过滤（tenant_scope_self_managed=True），理由：get_queryset 先算出当前用户可访问项目集合（Q(owner=user)|Q(members=user)），再以 projects__in 限定版本，不属于这些项目的版本一律不可见（fail-closed），即使 query_params 传入他人 projects 也只能在该集合内二次收窄；Version 只有 projects 这一 M2M 关联、无 organization/project 单外键，自动解析会退化为 created_by，把协作成员可见的共享版本收紧成"仅自己创建"，属功能倒退
- `apps/core_platform/views/versions.py:78` **VersionDetailView** — 已显式声明自管租户过滤（tenant_scope_self_managed=True），理由：同 VersionListCreateView：仅返回归属于当前用户 owner|members 项目的版本，他人项目的版本不可达（fail-closed）；Version 无 organization/project 单外键，自动解析会退化为 created_by，把协作成员共享的版本收紧为仅自己创建，属功能倒退
- `apps/defects/views.py:43` **DefectViewSet** — 已显式声明自管租户过滤（tenant_scope_self_managed=True），理由：get_queryset 显式实现组织级边界：管理员全量；有组织用户限 organization=本组织（与原 TenantScopedViewSetMixin 等价）；无组织用户（organization 为空）原实现会退化为 organization IS NULL，使所有无组织用户互相看到对方缺陷，此处收敛为仅 reporter/assignee/所属项目 owner|members 自己相关的缺陷，只收紧不放宽，同时避免自动解析在 org 为空时整体 fail-closed 清零、让用户看不到自己上报的缺陷
- `apps/performance_test/views.py:114` **PerformanceRequestViewSet** — 已显式声明自管租户过滤（tenant_scope_self_managed=True），理由：get_queryset 以 collection__project__in = Q(owner=user)|Q(members=user) 的 PerformanceProject 做归属过滤，未命中即空集（fail-closed），边界不弱于组织级隔离。PerformanceRequest 无 organization、亦无 project 直连字段，自动解析会落到 created_by，把项目成员共享的请求收成 creator-only、破坏协作，故声明自管
- `apps/ui_automation/views/task_views.py:713` **NotificationLogViewSet** — 已显式声明自管租户过滤（tenant_scope_self_managed=True），理由：已核实 NotificationLog.task_id 是裸 IntegerField（无外键，见 apps/notifications/models.py:77），模型无 organization / project / created_by 任一租户字段，自动解析必然 fail-closed 清零、让用户看不到自己任务的通知；写 task__xxx 多跳路径则直接 FieldError 500。故由 get_queryset 反查归属：取 Q(owner=user)|Q(members=user) 的 UiProject 名下的 UiScheduledTask 及 created_by=user 的任务，按 task_id__in 收敛，未命中即空集（fail-closed），边界不弱于组织级隔离
- `apps/ui_automation/views/testcase_views.py:91` **UiTestCaseModuleViewSet** — 已显式声明自管租户过滤（tenant_scope_self_managed=True），理由：get_queryset 以 super().get_queryset() 起步，完整继承 BaseProjectViewSet 的归属边界：携带 project_id 时校验该项目的 owner/members（传他人 project_id 返回空集），未携带时因 UiTestCaseModule 无 created_by 字段而直接 none()，两路均 fail-closed；其后仅追加 parent__isnull 层级筛选，不放宽任何可见性。模型有 project 外键但 UiProject 无 organization 字段，自动解析会命中 project__organization 导致 FieldError，故声明自管
