[pytest]
testpaths = tests
python_files = test_*.py
# 双租户隔离实测（tests/test_tenant_isolation.py）是 Django TestCase，依赖测试数据库，
# 统一交由 `manage.py test` 运行（CI 与本地口径一致）；这里从 pytest 收集范围排除，
# 避免 pytest 在无 pytest-django 环境下收集即报错、或重复跑。
addopts = -q -p no:cacheprovider --ignore=tests/test_tenant_isolation.py
