# 2026-07-27 工作日志

## 甲方验收评审（智能测试平台）
- 用户以"甲方公司"身份，要求对该自动化测试平台（E:\test\TC\aotutest\aotutest-new-main，Django+Vue3）做一次全面交付评审。
- 组建 8 位专家 agent 并行"very thorough"只读评审：产品经理、架构师、前端、后端、测试、运维、数据工程师、AI工程师。
- 输出《甲方验收评审报告.md》（项目根目录）。
- 核心结论：**不予通过，退回重大整改**。
  - P0 约 20 项：RCE(exec×3)、LLM-SQL 裸执行、硬编码 Key(`sk-5e83993f...`)、admin123、全局关 CSRF、默认 DEBUG=True、明文 CI 凭据、AllowAny 报告/上传、Strix 安全测试伪造、Midscene 死模块、AI Copilot 永远返回假回复、K8s 执行池纯模拟、容器 WSGI 路径错误无法启动、配置系统与 env 割裂、编排 Celery 但代码用 django-q2 且未装、Playwright 浏览器未装。
  - 14 项 AI 卖点仅约 6 项真实，8 项名不副实（Text-to-SQL 冒充 Vanna/RAG、多智能体 Orchestrator 单 LLM 调用、精准测试 mock、Midscene 脚本缺失、special_testing 首页纯假数据、知识图谱抽取 mock）。
  - 平台自身测试覆盖率≈0%，无 CI 流水线。
- 经验：多专家并行评审用 Explore 子代理 + run_in_background 高效；汇总时按"安全红线/功能伪造/部署阻断"重组 P0 去重。

注：该 workspace 还存在 BOOTSTRAP.md 未处理（身份引导未完成），用户当前聚焦交付评审，未触发身份对话。
