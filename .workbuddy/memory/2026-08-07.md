# 2026-08-07（续）

## 端到端演示脚本 + 集成测试（用户确认「需要」）
- 新增 `apps/eval_pod/management/commands/demo_eval_flow.py`：离线零外送端到端跑通
  需求→生成(B1)→评测→基线(B4)→门禁(C1 精确指指标)→分析(B3)→Trace 导出(C2)。
  幂等（get_or_create），支持 `--clean`/`--org-code`/`--thresholds-*`；演示数据落独立 `demo-eval` 租户。
- 修复：`_ensure_tenant` 三元表达式误返回元组 → 改为先 filter 再 create_user。`graders` 评分复用 RULE 确定性。
- 新增 `tests.py:EvalEndToEndTest`（2 项）：真实 API 链路
  - 劣化版：C1 门禁 BLOCK，failed_thresholds=[mean_score,pass_rate]，regressed_metrics 精确指出掉点；B4 判 regressed；B3 命中 RULE 模式。
  - 等价候选：门禁 PASS、B4 不判回归、B3 无系统性风险。
- 踩坑：`_run_with` 返回对象未 refresh_from_db → mean_score 读到 None；改为返回前 `run.refresh_from_db()`。
- 验证：eval_pod 测试 **47 tests OK**；`manage.py check` 无问题；`demo_eval_flow` 开发库实跑成功（4 用例，门禁 BLOCK 并精确指指标），跑后删 `demo-eval` 租户级联清理。
- 文档：roadmap 质量验证更新为 47 tests + 端到端演示说明。
- 待提交：以上改动连同上一轮 Phase B/C/A1 已推（233089c）；本轮需追加提交演示脚本+集成测试并 push。
