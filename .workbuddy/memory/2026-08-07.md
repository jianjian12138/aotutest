# 2026-08-07 工作日志（aotutest · 路线三全栈收尾）

## 本日交付：Phase B + Phase C + A1（路线三闭环）

### 源码改动（apps/eval_pod）
- `agents.py`：新增 `default_llm_call`（复用 `AIModelConfig.for_tenant` 的 `AIModelService` 调用）、`generate_cases`(B1)、`analyze_run`(B3)、`compare_to_baseline`(B4)、`eval_gate`(C1) —— 全部离线安全。
- `models.py`：给 `EvalRun` 加 `is_baseline`（B4 基线标记）。
- `views.py`：新增 @action —— `EvalDatasetViewSet.generate_cases`(B1)；`EvalRunViewSet.set_baseline`/`compare`(B4)/`analyze`(B3)/`gate`(C1)；`EvalTraceViewSet.export`(C2)。
  - **坑**：编辑 `generate_cases` 时误删了 `EvalCaseViewSet` 类头（被 `org_field/queryset/serializer_class` 三行变成游离语句），已修复。教训：替换块务必保留原类声明。
- `serializers.py`：`EvalRunSerializer` 暴露 `is_baseline`（read_only）。
- `tests.py`：新增 `PhaseBCTest`（11 项）覆盖 B1 离线/LLM升级/降级、B4 基线+对比、B3 分析、C1 阈值/回归/通过、C2 导出+隔离。

### 迁移
- `apps/eval_pod/migrations/0007_evalrun_is_baseline.py`（本地生成，gitignore 不入库）。

### 前端（A1）
- 新增 `frontend/src/api/eval.js`、`frontend/src/views/eval/EvalDashboard.vue`。
- `frontend/src/router/index.js` 加 `/eval` 路由；`frontend/src/layout/index.vue` 加「Agent 测评」菜单（currentModule=eval）。
- 验证：本机 `node_modules` 缺失 → `npm install`（569 包）后 `@vue/compiler-sfc` 单独编译 EvalDashboard/router/api/layout 均 OK。完整 `vite build` 因**既有无关文件** `src/views/configuration/UIEnvironmentConfig.vue` 误引 `Chrome` 图标报错（非本次改动），其余 2289 模块正常。

### CI（C3）
- 新增 `.github/workflows/eval-gate.yml`：push/PR 到 `agent-eval-v2` 跑评测舱测试套件作为质量门禁。

### 验证结果
- `manage.py test apps.eval_pod` → **45 tests OK**（原 34 + 新 11）。
- `manage.py check` → 无问题。

### 提交
- 待提交到 `agent-eval-v2`（分支当前即此）。不要提交 `node_modules`(已 ignore) 与 `migrations/*`(已 ignore)。
- 注意：测试环境必须用项目 `.venv311`（Django 4.2.7），不是 `.workbuddy/binaries/python/envs/default`（那是个缺依赖的 Django 6.0.7 残环境）。
