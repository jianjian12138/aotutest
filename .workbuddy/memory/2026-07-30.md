# 2026-07-30

## 第五轮甲方验收完成（🟡 有条件通过）
- 3 专家组并行复核 + 甲方独立复跑 GATE（compileall/check/migrations/validate_tenant_scope/pytest 27 全绿）。
- **第四轮整改确认真实有效**：34 ViewSet get_queryset 层隔离 0 失效、P0-新A/B/C 闭环、Vanna 真实建连确认。首次无"假绿"。
- 新发现三大战场（写入《第五轮正式验收报告.md》）：
  1. **@action 盲区**：7 类 @action 直连 Model.objects 绕过隔离（3 P0：data_factory generate:300/batch_refresh:663 跨租户用他人 DB 配置执行 SQL；req_analysis review_test_cases:512 改写他租户用例）。
  2. **8 个未审计 app**：19 视图中 13 全量泄露；P0=notifications/scheduler 序列化器 fields='__all__' 明
文返回 webhook secret+PII；reports 8 个聚合 action 查全表且 project_id 不校验归属。
  3. **系统性模拟残留**：10+ 处生产假实现（cicd mock pipeline、strix 模拟执行、ui_automation 伪造截图、req_analysis 模拟 LLM 写死 85 分等）。
- token 链路 🟡：主体闭环，唯一缺口 logout_view 不 delete_cookie('refresh_token')（Cookie 残留 7 天可续期）。
- 第六轮整改清单已列入报告第六节（P0×3 / P1×3 / P2×3），核心规范：所有 @action 禁止直接 Model.objects，应走 self.get_queryset()。

## 复盘：为何五轮未一次通过（甲乙双方根因）
- 结论：五轮中**没有一轮把上轮修好的改坏**（第五轮实测 0 失效）。问题增长源于"审计探照灯"逐轮放大：点(1-2轮功能) → 线(3轮租户隔离) → 面(4轮34视图铺开) → 体(5轮 @action维度 + 8个新app范围 + 反造假新标尺)。
- 甲方责任：验收标准逐轮追加，从未在首轮一次性冻结完整清单。
- 乙方惯性：按问题清单被动整改，而非按规范做全量自查。
- 共同缺失：无机器门禁，靠人工抽样审计。
- **终局方案（第六轮定为收敛轮）**：①先冻结验收标准清单（安全/隔离/真实性/质量四维，一次列全不再追加）②写全量扫描器替代人工抽样（扩展 scripts/validate_tenant_scope.py，增加 @action 直连 Model.objects 检测、serializer fields='__all__' 敏感字段检测、生产代码 mock/simulate 检测）③按扫描器输出的完整基线一次性修完 ④扫描器入 CI 门禁防回归。

## 四合一扫描器 + 第六轮基线（收敛轮启动）
- 新增 `scripts/audit_baseline.py`（AST 静态分析，flake8 0 违规）：D1 @action直连Model.objects数据流定性 / D2 序列化器敏感字段 / D3 生产mock残留 / D4 视图隔离接入覆盖 / D5(--orm) 租户路径ORM编译。支持 `--ci` 门禁模式（P0>0 退出1）。
- **D1 三轮规则精化**（P0: 116→32→57），剔除两类系统性误报：①create(外键=self.get_object()) 派生自已校验对象 ②create(content=request.data) 用户输入作内容字段。引入 trusted/untrusted/untrusted_obj 三态污点传播 + ID_LOOKUP/FK_KWARG 识别。随机抽检 8 个 P0，8/8 真实越权，基线可信。
- **全量基线**：421 项（P0=57 / P1=87 / P2=277）。输出 `安全审计基线.md` + `scripts/audit_baseline.json`。
- **颠覆性发现**：前五轮反复审的 7 个 app 仅占 86 项(P0 12)；**从未审计的 ui_automation(105)/api_testing(52)/testcases(42)/assistant(33)/performance_test(31) 占 263 项、P0 39 (63%)**。前五轮一直在打扫同一间房。
- 产出《第六轮收敛升级方案.md》：①验收标准冻结为扫描器四维（代码定义非人话定义）②修复零件先行——在 mixin 加 scoped_get/scoped_filter，47 个 D1 P0 机械替换而非手写47遍 ③三批整改 ④CI门禁+基线锁定+`# noqa: tenant-ok` 留痕豁免机制。

## 第六轮批次1完成：P0 57→0，CI门禁通过
- 零件：TenantAwareViewSetMixin 新增 scoped_get/scoped_filter/scoped_queryset + 模块级 scoped_queryset_for(user, model, org_field)（permissions.py）。
- D1 47项清零（3专家组并行机械替换）：VannaConfig/GeneratedTestCase 根越权点闭环；UiProject/ApiProject 无 organization → org_field='project__owner'；Environment→'created_by'；无租户字段模型走已校验父对象关系。
- D2 10项清零（亲自修）：webhook_bots/api_key/webhook_url 全部只写+掩码（统一 mask_webhook_bots() 在 notifications/serializers.py）；fields='__all__' 改白名单。
- D3 按甲方拍板：501处置12处（cicd假Pipeline/strix假执行/ui假run/K8s伪造Pod等）、真实化/删伪造8处（含写死85分评审）；断言旧假行为的测试同步改 501。
- 演示数据：scripts/seed_demo_data.py（幂等）插入13项双租户数据；账号 demo_admin/demo_user_a/demo_user_b，密码 Demo@12345；Organization 需 code 唯一键（DEMO-ORG-A/B）、TestCase 属主字段是 author。
- GATE 7项全绿：compileall/flake8/check/migrations/validate_tenant_scope/audit_baseline --ci(P0=0退出0)/pytest 27。
- 遗留副作用（批次2）：UiDevice 无租户字段 fail-closed 降级；ui_automation 可见性收紧为 owner-only；P1=72、P2=265 待批次2/3。
- 产出《第六轮批次1整改报告.md》。
