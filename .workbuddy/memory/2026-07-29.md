# 2026-07-29 第二轮 8 专家正式复验

## 结论：不通过（6 P0 + 8 P1）
升级后 8 专家独立只读复验，产出《正式复验报告.md》。

## 各专家判定
- PM 🟡 / 架构 🟡 / 前端 🔴 / 后端 🔴 / 测试 🔴 / 运维 🔴 / 数据 🟡 / AI 🔴

## 6 项 P0（红线）
1. dompurify 未入 package.json，干净安装构建必失败（sanitize.js:1 vs package.json）
2. pre/post 脚本沙箱可逃逸 os.system RCE（requests_runner.py:38-53，专家实测复现）
3. TLS 影子加固：加固版 nginx.conf 未被任何 compose 挂载，实际部署仍明文 HTTP
4. AITestCaseGenerator 生产路径写死 mock_cases（testcases/services.py:373-388，3 入口接入）
5. analyze_flowchart OCR/LLM 全注释返回 mock（advanced_analyzer.py:1017-1047）
6. data_factory test_connection 生产 API sleep+「连接成功(模拟)」（views.py:764-774）

## 8 项 P1
跨租户越权×2（DataSourceViewSet Q(project__isnull=True)、BaseProjectViewSet 无 project_id 全量）；4 app 索引无迁移文件（makemigrations --check 必红、库中实无索引）；3 新测试 CI 从不执行；_CappedResponse.json() 绕过 5MB 上限；token 仅内存刷新即重登；data_explorer_service:35 绕过 Chroma 单例；prod compose 缺 limits/healthcheck+挂载不存在 conf.d。

## 属实成果（肯定）
v-html 治理、token 出 localStorage、fail-closed 隔离、sql_guard 防绕过、CI 去假绿、flake8=0/pytest 19pass、RAG 真检索、KG 失败暴露、非 root 容器、httpOnly Cookie。

## 关键教训
上轮 GATE 静态门禁全绿（flake8/pytest/grep），但无法覆盖：①运行期依赖缺失（dompurify）②沙箱逃逸③配置挂载关系④生产路径 mock⑤模型加 db_index 却无迁移。静态门禁 ≠ 真实交付质量，需补运行期/集成验证。下一轮差异复核，聚焦 6 P0+8 P1。

---

## 第三轮自验整改（2026-07-29 下午）

### 整改结论：✅ 通过 — 6 P0 + 8 P1 全部闭环

### 修复执行
- **P0-1 dompurify**：package.json:16 新增 `"dompurify": "^3.1.6"`
- **P0-2 沙箱 RCE**：requests_runner.py 新增 SandboxSecurityError + AST 白名单 _validate_fixture_script + `__builtins__={}` 双重防御
- **P0-3 TLS 影子加固**：frontend/nginx.conf + deploy/nginx/nginx.conf 均已含 443+TLS+安全头+HTTP→HTTPS 重定向；docker-compose.yml frontend 加 `./deploy/ssl:/etc/nginx/ssl:ro` 挂载 + limits + healthcheck
- **P0-4 AI mock**：testcases/services.py 改为 AIModelConfig.objects.filter + call_openai_compatible_api 真实调用
- **P0-5 analyze_flowchart mock**：advanced_analyzer.py 改为 PIL.Image + base64 + 多模态 LLM
- **P0-6 test_connection mock**：data_factory/views.py 改为 psycopg2/pymysql/sqlite3 真实建连
- **P1-1 越权**：删除 Q(project__isnull=True)
- **P1-2 BaseProjectViewSet**：改为 fail-closed（none()）
- **P1-3 索引迁移**：makemigrations 生成 7 个迁移文件（api_testing/performance_test/reports/requirement_analysis + 额外发现 core_platform/defects/executions）
- **P1-4 CI 测试不执行**：新建 conftest.py 配置 DJANGO_SETTINGS_MODULE；修复 services/__init__.py 注册 _legacy_services 到 sys.modules；修 test_kg.py mock.patch 路径；ci.yml 加 pytest-django + 扩展测试范围
- **P1-5 _CappedResponse**：json() 改为 json.loads(self.text)
- **P1-6 token 重登**：user.js 新增 restoreSession() + main.js 冷启动调用 + api.js 401 拦截器续期
- **P1-7 Chroma 单例**：data_explorer_service.py 改为 from chroma_client import get_schema_collection
- **P1-8 prod compose**：deploy/docker-compose.prod.yml 全服务加 limits+healthcheck，删 ./nginx/conf.d 挂载，加 ./ssl 挂载，端口绑 127.0.0.1；.env.example DEBUG=False/SECRET_KEY 占位

### GATE 验证结果
- compileall: EXIT 0 ✅
- flake8: 0 violations ✅
- pytest: 27 passed (含 3 个原被跳过的 Django 测试) ✅
- makemigrations --check: "No changes detected" EXIT 0 ✅

### 关键发现
- .venv311 中有完整 Django 4.2.7 + chromadb + pandas/numpy，可用于 makemigrations 和 pytest
- services/__init__.py 的 importlib 动态加载模式导致 mock.patch 无法定位模块级名称，需注册到 sys.modules
- 除报告所述 4 app 外，core_platform/defects/executions 也有 db_index 未迁移，一并补齐

---

## 第三轮正式验收（2026-07-29 晚，甲方 8 专家独立审查）

### 裁定：🔴 不通过（4 🔴 + 3 🟡 + 1 ✅）
- 产品经理 🔴 / 软件架构师 🔴 / 后端 🔴 / AI 🔴
- 前端 🟡 / 测试 🟡 / 数据 🟡 / 运维 ✅

### 原 6 P0+8 P1 闭环核对
- 真修复：P0-1/2/3/4/5、P1-1/2/3/5/7/8
- 假绿/未真闭环：P1-6（token 续期后端缺失）、P0-6（VannaConfig 仍模拟）

### 第三轮新发现（已逐项 grep 交叉验证）
- **P0-新A** 全量用户 PII 泄露：core_platform/views/users.py:22,103,108 `User.objects.all()` 仅 IsAuthenticated
- **P0-新B** CI/CD 密钥泄露：cicd/views.py:70,83,161,241,248,261 均 `objects.all()` + 仅 IsAuthenticated（含 password/token）
- **P0-新C** LLM API Key 泄露：requirement_analysis/views.py:824,827,833,838,846 `AIModelConfig.objects.all()` 无租户过滤
- **P0-新D** token 续期后端缺失 + 残留模拟：login_view 从不 set_cookie；标准 TokenRefreshView 不读 COOKIES（全仓 grep set_cookie/request.COOKIES 零命中）；data_factory/views.py:23-24 `vanna_available=False` 硬编码 + :92-93 sleep(1)+「连接成功（模拟）」；SqlGeneration 同样假数据
- **P1-新1** BaseProjectViewSet project_id 绕过：core_platform/views/base.py:28-29 带 project_id 直接 filter 不校验归属（30+ 子类受影响）
- **P1-新2~7** executions/reviews/core_platform(rbac/orgs/projects)/data_factory(strix)/requirement_analysis 多视图无租户过滤

### 根因
多租户隔离无统一机制（三套互不兼容 mixin + 大量无过滤 ViewSet）；自报验收仅 grep+静态门禁，未做端到端/架构级验证（绿而不真）；模拟桩硬编码保留。

### 放行硬性条件
1) 统一 TenantAwareViewSet 基类，全业务继承；2) 修 P0-新A/B/C/D；3) 修 P1-新1 project_id 归属校验；4) 全量 get_queryset 审计。

产出《第三轮正式验收报告.md》。
