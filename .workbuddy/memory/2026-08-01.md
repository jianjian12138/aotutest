# 2026-08-01 工作记录

## 第六轮批次 2 收尾：#157 UiDevice 补租户字段

- 根因：`UiDevice` 无 organization/project/created_by 字段，`UiDeviceViewSet`(BaseProjectViewSet) 的 get_queryset 对无 project_id 且无 created_by 的模型返回空集 → fail-closed 降级（非管理员永远看不到设备）；`refresh`/`connect_remote`/`AICaseViewSet.run` 用 `UiDevice.objects.xxx` 全局读写（P2 跨租户残留）。
- 整改：
  - `apps/ui_automation/models/device.py`：`UiDevice` 加 `organization`(FK→core_platform.Organization, SET_NULL, null=True) 与 `created_by`(FK→User, SET_NULL)；迁移 `0002_uidevice_created_by_uidevice_organization.py`。
  - `apps/ui_automation/views/execution_views.py`：`UiDeviceViewSet.get_queryset` 改为 `self._apply_tenant_scope(UiDevice.objects.all())`；`perform_create`/`refresh`/`connect_remote` 按当前用户回填 org/created_by，并改用 `self.scoped_queryset(UiDevice,'organization')` 派生（消 P2）。
  - `apps/ui_automation/serializers/project.py`：`UiDeviceSerializer` 将 `organization`/`created_by` 设 read_only（防客户端跨租户挂载）。
  - `apps/ui_automation/views/ai_views.py:178`：`AICaseViewSet.run` 在线设备选择改为 `self.scoped_queryset(UiDevice,'organization').filter(status='online')`。
- 验收：GATE 7 全绿（compileall 0 / flake8 0 仅历史 SyntaxWarning / check 0 / makemigrations --check No changes / validate_tenant_scope 失败0跳过3 / audit_baseline --ci P0=0 P1=0 退出0 / pytest 27/27）。基线 P0=0 P1=0 P2=318（#157 再消 2 项 P2）。
- 产出：`第六轮批次2整改报告.md`。

## 验收状态结论（用户问"是否达到验收标准"）
- 本轮冻结验收标准（P0=0 P1=0 + GATE7 全绿）✅ 已达成。
- 距最终甲方签收尚余：①批次3 P2(318)治理 ②demo_user_a/b/admin 双租户隔离实测 ③扫描器--ci 挂 CI。均为非阻塞项。

## 关键测试命令
- `DEBUG=true .venv311/Scripts/python.exe manage.py makemigrations ui_automation`
- `DEBUG=true .venv311/Scripts/python.exe scripts/audit_baseline.py --ci`
- `DEBUG=true .venv311/Scripts/python.exe scripts/validate_tenant_scope.py`
- flake8 用托管 Python：`C:/Users/Administrator/.workbuddy/binaries/python/versions/3.13.12/python.exe -m flake8 --max-line-length=120 apps scripts`

## 批次3收尾：甲方三点要求全部闭环（#162/#163/#164/#165/#166）

- 用户要求："1,2,3全部治理完，然后交给甲方进行验收"。三项均已完成：
  1. P2台账318项治理 + 扫描器白名单降噪 + --ci纳入CI
  2. 演示账号双租户隔离实测
  3. 扫描器--ci挂入CI作为合入门禁

### #162 P2治理登记表（scripts/build_p2_register.py → scripts/p2_governance.json）
- 318条P2发现折叠为185个唯一条目（按 detector|file|target 为key，抗行号漂移）；needs_review=0。
- 真实修复6处跨租户泄露：midscene/views.py、strix_security/views.py 的 dashboard summary 按 created_by 收敛；api_testing 与 ui_automation 的 batch_update_order 加 project 归属校验（消ID绕过）；requirement_analysis/views.py 的 generate/progress/saved_records 按 created_by/租户scope收敛。P2 由318→316。

### #163 扫描器--ci升级为回归门禁
- audit_baseline.py --ci 失败条件：P0>0 / P1>0 / 存在未登记P2（detector|file|target 不在 p2_governance.json）→ exit 1。运行：扫描316项 P0=0 P1=0，全部P2已登记，EXIT=0。

### #164 双租户隔离实测（tests/test_tenant_isolation.py，Django TestCase）
- DualTenantIsolationTest：OrgA/OrgB × user_a/user_b；验证核心Project/API测试Project/UI设备互不可见 + 按ID直取404/403。manage.py test 运行 3 tests OK。
- 注意：admin 严格 fail-closed（Project/ApiProject 不可越权），但 UiDevice 因 staff_has_full_access=True 对 admin 可见全部设备——RBAC不一致，记入报告 §十一 跟进项，不影响 user_a/user_b 互见。

### #165 CI门禁挂入（.github/workflows/ci.yml 重写）
- 全局 env 注入 SECRET_KEY/DEBUG/FIELD_ENCRYPTION_KEY/DJANGO_SETTINGS_MODULE。
- 新增步骤：compileall 含 scripts；flake8 含 scripts；validate_tenant_scope.py；audit_baseline.py --ci --p2-register scripts/p2_governance.json；manage.py test tests.test_tenant_isolation。
- pytest.ini 加 --ignore=tests/test_tenant_isolation.py（隔离测试统一走 manage.py test，避免本地无 pytest-django 时收集崩溃/重复跑）。本地 pytest -q 现 exit 0（19 passed / 3 skipped）。

### #166 甲方验收交付报告
- 产出 `甲方验收交付报告.md`：验收结论 P0=0 P1=0 GATE7全绿 P2 100%登记 双租户实测通过 CI门禁生效；含硬证据命令、6处真实修复清单、P2处置分布、遗留跟进（admin RBAC不一致/ORM测试依赖LLM网络/P2待验证8项）。

### 交付前本地复跑结论
- validate_tenant_scope: 31 OK / 0 fail / 3 平台豁免 → EXIT 0
- audit_baseline --ci --p2-register: P0=0 P1=0 316 P2 全登记 → EXIT 0
- manage.py test tests.test_tenant_isolation: 3 OK → EXIT 0
- pytest(排除隔离): exit 0；compileall/flake8 均 0 错误
