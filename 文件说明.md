# 项目文件说明

本文档详细说明了 `d:\TEST` 项目中各个文件夹和文件的作用与功能。

## 1. 根目录文件

*   **`manage.py`**: Django 项目的管理脚本，用于运行服务器、执行迁移、创建用户等（如 `python manage.py runserver`）。
*   **`requirements.txt`**: Python 项目依赖列表，列出了项目运行所需的所有 Python 包及其版本。
*   **`README.md`**: 项目说明文档，通常包含项目介绍、安装步骤和使用指南。
*   **`.gitignore`**: Git 版本控制忽略文件配置，指定了哪些文件不需要上传到代码仓库（如虚拟环境、临时文件等）。
*   **`__init__.py`**: 标识该目录为一个 Python 包。
*   **`db.sqlite3`**: Django 默认的 SQLite 数据库文件，存储了项目的关系型数据（用户、项目、用例等）。

## 2. 主要目录结构

### `backend/` (后端配置)
Django 项目的核心配置目录，相当于项目的“控制中心”。
*   **`settings.py`**: 项目的全局配置文件（数据库配置、安装的应用、中间件、静态文件路径等）。
*   **`urls.py`**: 项目的根路由配置，定义了 URL 请求如何分发到各个 App。
*   **`wsgi.py`**: WSGI 兼容的 Web 服务器入口，用于部署生产环境。
*   **`asgi.py`**: ASGI 兼容的 Web 服务器入口，用于支持异步功能（如 WebSocket）。
*   **`celery.py`**: Celery 异步任务队列的配置入口。

### `apps/` (业务应用)
存放所有业务逻辑模块（Django Apps），每个文件夹代表一个功能模块。
*   **`users/`**: 用户管理模块（登录、注册、权限）。
*   **`projects/`**: 项目管理模块。
*   **`api_testing/`**: 接口测试核心模块。
*   **`ui_automation/`**: UI 自动化测试模块（包含 Playwright/Selenium 封装）。
*   **`performance_test/`**: 性能测试模块。
*   **`testcases/`**: 测试用例管理模块。
*   **`testsuites/`**: 测试套件管理模块。
*   **`executions/`**: 测试执行记录模块。
*   **`reports/`**: 测试报告生成模块。
*   **`data_factory/`**: 数据工厂模块（SQL 生成、造数）。
*   **`knowledge_graph/`**: 知识图谱与 AI 助手模块。
*   **`strix_security/`**: 安全测试模块。
*   **`scheduler/`**: 定时任务调度模块。
*   **`configuration/`**: 系统配置中心（AI 模型配置、环境配置等）。
*   **`assistant/`**: AI 助手核心逻辑。
*   **`requirement_analysis/`**: 需求分析与 AI 用例生成模块。
*   **`reviews/`**: 用例评审模块。
*   **`versions/`**: 版本管理模块。

### `frontend/` (前端源码)
Vue.js 前端项目目录。
*   **`src/`**: 源代码目录。
    *   **`views/`**: 页面视图组件，与 `apps` 中的模块大致对应。
    *   **`api/`**: 后端接口调用封装。
    *   **`router/`**: 前端路由配置。
    *   **`layout/`**: 页面布局组件（侧边栏、顶部导航）。
    *   **`components/`**: 公共组件。
    *   **`stores/`**: Pinia 状态管理（如用户信息）。
*   **`package.json`**: 前端项目依赖配置。
*   **`vite.config.js`**: Vite 构建工具配置。

### `media/` (媒体文件)
存放用户上传的文件和系统生成的动态文件。
*   **`allure-reports/`**: 存放生成的 Allure 测试报告（HTML）。
*   **`allure-results/`**: 存放测试执行的原始结果数据（JSON）。
*   **`ai_recording/`**: 存放 AI 自动化测试执行时的屏幕录制（GIF）。
*   **`knowledge_docs/`**: 存放知识库文档。

### `tools/` (工具脚本)
存放辅助开发的脚本工具。
*   **`recorder_agent.py`**: Playwright 录制代理脚本，用于在客户端录制浏览器操作并生成用例。

### `logs/` (日志)
存放系统运行日志文件（目前仅包含 `.gitkeep` 占位符）。

### `allure/` (Allure 工具)
存放 Allure 命令行工具及其插件，用于生成可视化的测试报告。
*   **`bin/`**: 可执行文件。
*   **`lib/`**: 依赖库。
*   **`plugins/`**: 各种报告插件。

### `chroma_db/` (向量数据库)
用于存放 ChromaDB 的持久化数据（向量索引），支持 AI 知识库的检索功能。
